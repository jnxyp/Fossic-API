services:
  fossic-api:
    build: .
    restart: unless-stopped
    ports:
      - "127.0.0.1:8000:8000"
    volumes:
      - ./src:/app
    env_file:
      - .env
    extra_hosts:
      - "host.docker.internal:host-gateway"
